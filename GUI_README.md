# GUI 使用说明

## GUI 界面布局

AI 逆转裁判的 GUI 使用 CustomTkinter 构建，提供现代化的图形界面。

### 主界面组成

```
┌─────────────────────────────────────────────────────┐
│                  AI 逆转裁判                         │
├─────────────────────────────────────────────────────┤
│                                                       │
│  庭审过程：                                          │
│  ┌───────────────────────────────────────────────┐  │
│  │                                                │  │
│  │  [实时显示庭审过程的文本输出区域]               │  │
│  │                                                │  │
│  │  - 案件信息                                    │  │
│  │  - 调查阶段                                    │  │
│  │  - 庭审对话                                    │  │
│  │  - 角色发言                                    │  │
│  │  - 判决结果                                    │  │
│  │                                                │  │
│  │  [自动滚动到最新内容]                          │  │
│  │                                                │  │
│  └───────────────────────────────────────────────┘  │
│                                                       │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐             │
│  │开始庭审│  │清空输出│  │  退出  │             │
│  └─────────┘  └─────────┘  └─────────┘             │
│                                                       │
│  状态：就绪                                          │
└─────────────────────────────────────────────────────┘
```

## 功能说明

### 1. 开始庭审按钮
- 点击后启动一次完整的法庭模拟
- 庭审过程中按钮会被禁用，防止重复点击
- 庭审结束后自动恢复可用状态

### 2. 清空输出按钮
- 清空文本显示区域的内容
- 方便开始新的庭审

### 3. 退出按钮
- 关闭 GUI 窗口
- 自动恢复标准输出

### 4. 文本显示区
- 实时显示庭审过程
- 自动滚动到最新内容
- 移除 ANSI 颜色代码，使用 GUI 原生样式

### 5. 状态栏
- 显示当前程序状态
- "就绪"：可以开始庭审
- "庭审进行中..."：正在运行模拟

## 特色功能

### 双重输出
GUI 模式下的输出会**同时**显示在：
1. GUI 窗口的文本框中
2. 启动程序的控制台终端中

这样设计的好处：
- GUI 窗口提供友好的用户界面
- 控制台输出便于调试和日志记录
- 可以同时查看两处的输出

### 线程安全
- 庭审模拟在独立线程中运行
- GUI 界面保持响应，不会冻结
- 可以随时清空输出或退出程序

## 使用建议

### 正常使用
```bash
# 直接启动（默认GUI模式）
python main.py

# 明确指定GUI模式
python main.py --gui
```

### 无图形环境
如果在服务器或无图形环境中运行，使用控制台模式：
```bash
python main.py --console
```

### 调试模式
在 config.py 中设置：
```python
DEBUG_MODE = True  # 显示更多调试信息（如真凶）
SHOW_API_LOGS = True  # 显示 API 调用日志
```

调试信息会同时出现在 GUI 和控制台中。

## 技术细节

### 输出重定向机制
使用 `TeeOutput` 类实现双重输出：
- 继承标准输出接口
- 同时写入控制台和 GUI 文本框
- 自动移除 ANSI 转义序列

### 颜色处理
- 控制台使用 colorama 提供彩色输出
- GUI 自动移除 ANSI 代码，使用纯文本显示
- 保证两端输出都可读

### 异步执行
- GUI 主循环在主线程运行
- 庭审模拟在子线程执行
- 通过 `after()` 方法安全更新 GUI
