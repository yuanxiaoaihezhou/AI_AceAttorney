# 使用指南

## 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 配置 LLM

打开 `config.py` 文件，根据您的需求选择 LLM 提供商：

#### 选项 A: 使用 Ollama（推荐用于本地部署）

```python
LLM_PROVIDER = "ollama"
OLLAMA_MODEL_NAME = "qwen2:7b"  # 或 qwen2:4b, qwen2.5:7b 等
```

**安装 Ollama:**
1. 访问 https://ollama.ai 下载并安装 Ollama
2. 下载模型: `ollama pull qwen2:7b`
3. 启动服务: `ollama serve`

#### 选项 B: 使用 SiliconFlow（在线 API）

```python
LLM_PROVIDER = "siliconflow"
SILICONFLOW_API_KEY = "sk-your-api-key"  # 填入你的 API Key
```

**获取 API Key:**
访问 https://cloud.siliconflow.cn 注册并获取 API Key

### 3. 运行程序

**基础版（直接庭审）:**
```bash
python AI_court.py
```

**增强版（包含调查阶段）:**
```bash
python AI_court_enhanced.py
```

## 游戏流程

### 基础版 (AI_court.py)

1. **案件生成**: AI 自动创建案件，包括被告、证人、证物等
2. **查看案件信息**: 显示案件背景和证物清单
3. **开庭审理**: 
   - 证人作证（通常包含谎言）
   - 律师分析证词并指证或威慑
   - 检察官反驳
   - 证人回应
   - 法官裁决
4. **判决**: 证人崩溃后，法官宣判被告无罪

### 增强版 (AI_court_enhanced.py)

1. **案件生成**: 生成更详细的案件，包括多个地点和 NPC
2. **调查阶段**: 
   - 查看可调查的地点
   - 查看可询问的 NPC
   - 自动收集所有证物（简化版）
3. **庭审阶段**: 与基础版相同

## 配置参数说明

### LLM 配置

- `LLM_PROVIDER`: LLM 提供商（"ollama" 或 "siliconflow"）
- `OLLAMA_MODEL_NAME`: Ollama 模型名称
- `SILICONFLOW_MODEL_NAME`: SiliconFlow 模型名称

### 庭审配置

- `MAX_ROUNDS`: 最大回合数，防止死循环（默认: 10）
- `WITNESS_BREAKDOWN_THRESHOLD`: 证人被指证多少次后会崩溃（默认: 3）

### LLM 参数

- `DEFAULT_TEMPERATURE`: 生成文本的随机性，0.0-2.0（默认: 1.0）
  - 值越低，输出越确定
  - 值越高，输出越有创造性
- `JSON_TEMPERATURE`: 生成 JSON 时的温度（默认: 0.7）
- `MAX_TOKENS`: 最大生成 token 数（默认: 1024）

### 调试配置

- `DEBUG_MODE`: 调试模式，显示更多信息（默认: False）
- `SHOW_API_LOGS`: 显示 API 调用日志（默认: False）

## 角色说明

### 案件设计者 (CaseDesigner)
- 自动生成案件剧情
- 设计证物和证人证词
- 确保证词中包含可被反驳的谎言

### 法官 (Judge)
- 控制庭审流程
- 评估辩论情况
- 在证人崩溃时做出判决

### 辩护律师 (DefenseAttorney)
- 分析证人证词
- 寻找证词与证物的矛盾
- 使用"指证"或"威慑"追查真相

### 检察官 (Prosecutor)
- 维护起诉立场
- 反驳律师的指控
- 保护证人的证词

### 证人 (Witness)
- 通常是真凶
- 作伪证试图隐瞒真相
- 被追问到无法自圆其说时会崩溃

### 警探 (Detective) [仅增强版]
- 协助调查
- 提供案件信息
- 分享调查发现

## 操作指南

### 律师行动

律师 AI 会自动做出以下决策：

1. **【威慑】**: 当没有明显矛盾时，追问细节
2. **【指证：证物名】**: 当发现证词与证物矛盾时，提出证物反驳

### 判决条件

法官会在以下情况做出判决：
- 证人被指证 3 次以上（可在 config.py 中调整）
- 证人说出"我承认"或"是我做的"等关键词
- 证人表现出明显的崩溃（语无伦次、大喊大叫等）

## 常见问题

### Q: 为什么连接失败？

**使用 Ollama:**
- 确保 Ollama 服务已启动: `ollama serve`
- 检查模型是否已下载: `ollama list`
- 确认 API URL 正确（默认: http://localhost:11434/api/chat）

**使用 SiliconFlow:**
- 检查 API Key 是否正确
- 确认网络连接正常
- 检查是否有 API 调用配额

### Q: 案件生成失败怎么办？

- 检查 LLM 响应是否正常
- 尝试降低 `JSON_TEMPERATURE` 参数
- 开启 `DEBUG_MODE` 查看详细信息

### Q: 庭审总是超时怎么办？

- 增加 `MAX_ROUNDS` 参数
- 降低 `WITNESS_BREAKDOWN_THRESHOLD` 参数
- 检查证物描述是否足够详细

### Q: 如何更换模型？

**Ollama:**
```bash
# 下载新模型
ollama pull qwen2.5:14b

# 修改 config.py
OLLAMA_MODEL_NAME = "qwen2.5:14b"
```

**SiliconFlow:**
修改 config.py 中的 `SILICONFLOW_MODEL_NAME`

### Q: 可以同时使用多个 LLM 吗？

目前不支持。您需要在 `config.py` 中选择一个提供商。

## 进阶使用

### 自定义案件难度

修改 `CaseDesigner.generate_case()` 方法中的 prompt，可以调整案件的复杂度：
- 增加证物数量
- 添加更多证人
- 设计更隐蔽的谎言

### 添加新角色

参考 `AIAgent` 基类，创建新的角色类：

```python
class NewRole(AIAgent):
    def __init__(self):
        super().__init__("角色名", """
        你是xxx角色。
        你的职责是...
        """)
```

### 调整 AI 性格

修改各个角色类中的 `role_prompt` 可以调整 AI 的行为和性格。

## 贡献指南

欢迎提交 Issue 和 Pull Request！

请确保：
1. 代码符合 PEP 8 规范
2. 添加必要的注释
3. 测试新功能
4. 更新相关文档

## 许可证

MIT License
